p = linspace([0.5 0.2 0.1],[0.005 0.002 0.001],21);
while j = 6
    
pb = zeros(1,size(p,2));
seed = 10;
s = rng(seed);
numeroBits = 4*250000;
bitInformacaoPalavra = 4;
q = 0.5;
for i = 1 : size(p,2)
    pb(i) = Erros(numeroBits,bitInformacaoPalavra,q,p(i),randi([0,2^31]))/numeroBits;
    %pb(i) = Erros(numeroBits,bitInformacaoPalavra,q,p(i),randi([0,2^31]));
end
plot(p,pb);